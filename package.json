{
  "name": "funds-tracker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "cd api && pnpm install && cd ../webapp && pnpm install",
    "prepare": "husky install .husky",
    "start:webapp": "nx start funds-tracker-webapp"
  },
  "devDependencies": {
    "husky": "8.0.1",
    "nx": "14.3.5",
    "@nrwl/nx-cloud": "latest"
  },
  "workspaces": [
    "api",
    "webapp"
  ],
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0"
  }
}
