{
  "name": "funds-tracker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install:all": "pnpm install && cd webserver && pnpm install && cd ../webapp && pnpm install",
    "install:deployment": "cd webserver && pnpm install && cd ../webapp && pnpm install",
    "install:webserver": "pnpm install && cd webserver && pnpm install",
    "install:webapp": "pnpm install && cd webapp && pnpm install",
    "prepare": "husky install .husky",
    "lint:webserver": "cd webserver && pnpm lint",
    "lint:webapp": "cd webapp && pnpm lint",
    "lint": "pnpm lint:webserver && pnpm lint:webapp",
    "tsc:webserver": "cd webserver && pnpm tsc",
    "tsc:webapp": "cd webapp && pnpm tsc",
    "tsc": "pnpm tsc:webserver && pnpm tsc:webapp",
    "test:webserver": "cd webserver && pnpm test",
    "test:webserver-ci": "cd webserver && pnpm test:ci",
    "test:webapp": "cd webapp && pnpm test",
    "test": "pnpm test:webserver && pnpm test:webapp",
    "test:ci": "pnpm test:webserver-ci && pnpm test:webapp",
    "build:webserver": "cd webserver && pnpm build",
    "build:webapp": "cd webapp && pnpm build",
    "check:circular-deps": "cd webapp && pnpm check:circular-deps && cd ../webserver && pnpm check:circular-deps",
    "build": "pnpm build:webserver && pnpm build:webapp",
    "prisma:generate": "cd webserver && pnpx prisma generate"
  },
  "devDependencies": {
    "husky": "8.0.3"
  }
}
