export const { API_PORT } = process.env;
export const { AT_SECRET } = process.env;
export const { RT_SECRET } = process.env;
export const { RECAPTCHA_SECRET } = process.env;
export const IS_DEVELOPMENT = process.env.NODE_ENV !== 'production';
