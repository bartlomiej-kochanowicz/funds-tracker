export const { MONGO_HOSTNAME } = process.env;
export const { MONGO_PORT } = process.env;
export const { MONGO_DATABASE } = process.env;
export const { MONGO_USERNAME } = process.env;
export const { MONGO_PASSWORD } = process.env;
export const { API_PORT } = process.env;
export const IN_DEVELOPMENT = process.env.NODE_ENV !== 'production';
