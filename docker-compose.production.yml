version: "3.9"

services:
  web:
    image: nginx
    volumes:
      - ./templates:/etc/nginx/templates
    ports:
      - "80:80"
      - "443:443"
    environment:
      - NGINX_HOST=funds-tracker.com
      - NGINX_PORT=80
