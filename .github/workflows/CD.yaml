name: Post PR Deployment
on:
  push:
    branches:
      - master
jobs:
  merged_to_master:
    name: CD-app
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello World"
