import { FC } from 'react';

interface IInstrumentDetailsProps {
  instrument: string;
  date: Date;
}

export const InstrumentDetails: FC<IInstrumentDetailsProps> = ({ instrument, date }) => {
  return null;
};
